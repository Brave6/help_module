<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.e.help_module">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Help_module"
        tools:targetApi="31">
        <activity
            android:name=".HelpModuleFragment"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GlActivity">android</activity>
        <activity android:name=".FaqsActivity">android</activity>
        <activity android:name=".InfoActivity">android</activity>
        <activity android:name=".ErActivity">android</activity>
        <activity android:name=".CrActivity">android</activity>

    </application>

</manifest>